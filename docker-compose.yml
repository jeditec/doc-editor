services:
  doc-editor:
    image: ghcr.io/jeditec/doc-editor:latest
    container_name: doc-editor
    ports:
      - "8040:8040"
    volumes:
      - doc-data:/app/data
    environment:
      - PORT=8040

volumes:
  doc-data:
